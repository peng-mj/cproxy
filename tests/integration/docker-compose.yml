services:
  apt-tester:
    image: debian:bookworm-slim
    container_name: scproxy-apt-test
    environment:
      - PROXY_HOST=host.docker.internal
      - PROXY_PORT=18800
    volumes:
      - ./runners:/scripts:ro
      - ./reporters/results:/results
    extra_hosts:
      - "host.docker.internal:host-gateway"
    entrypoint: ["bash", "/scripts/apt_test.sh"]
    networks:
      - scproxy-test

  # pip-tester:
  #   image: python:3.12-slim
  #   container_name: scproxy-pip-test
  #   environment:
  #     - PROXY_HOST=host.docker.internal
  #     - PROXY_PORT=18800
  #   volumes:
  #     - ./runners:/scripts:ro
  #     - ./reporters/results:/results
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   entrypoint: ["bash", "/scripts/pip_test.sh"]
  #   networks:
  #     - scproxy-test

  go-tester:
    image: scproxy-test-env:latest
    container_name: scproxy-go-test
    environment:
      - PROXY_HOST=host.docker.internal
      - GO_PORT=18803
    volumes:
      - ./runners:/scripts:ro
      - ./reporters/results:/results
    extra_hosts:
      - "host.docker.internal:host-gateway"
    working_dir: /tmp/gotest
    entrypoint: ["bash", "/scripts/go_test.sh"]
    networks:
      - scproxy-test

  npm-tester:
    image: scproxy-test-env:latest
    container_name: scproxy-npm-test
    environment:
      - PROXY_HOST=host.docker.internal
      - NPM_PORT=18802
    volumes:
      - ./runners:/scripts:ro
      - ./reporters/results:/results
    extra_hosts:
      - "host.docker.internal:host-gateway"
    entrypoint: ["bash", "/scripts/npm_test.sh"]
    networks:
      - scproxy-test

  cargo-tester:
    image: scproxy-test-env:latest
    container_name: scproxy-cargo-test
    environment:
      - PROXY_HOST=host.docker.internal
      - PROXY_PORT=18800
    volumes:
      - ./runners:/scripts:ro
      - ./reporters/results:/results
    extra_hosts:
      - "host.docker.internal:host-gateway"
    working_dir: /tmp/cargotest
    entrypoint: ["bash", "/scripts/cargo_test.sh"]
    networks:
      - scproxy-test

  github-tester:
    image: scproxy-test-env:latest
    container_name: scproxy-github-test
    environment:
      - PROXY_HOST=host.docker.internal
      - GITHUB_PORT=18801
    volumes:
      - ./runners:/scripts:ro
      - ./reporters/results:/results
    extra_hosts:
      - "host.docker.internal:host-gateway"
    entrypoint: ["bash", "/scripts/github_test.sh"]
    networks:
      - scproxy-test

networks:
  scproxy-test:
    driver: bridge
